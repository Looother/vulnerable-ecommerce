<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html/src

    # Enable directory listing for DocumentRoot
    <Directory /var/www/html/src>
        Options +Indexes +FollowSymLinks
        AllowOverride None
        Require all granted
    </Directory>

    # Disable directory index files to force directory listing
    DirectoryIndex disabled

    # Map /uploads to the writable directory outside the mounted src volume
    Alias /uploads /var/www/html/uploads
    <Directory /var/www/html/uploads>
        Options +Indexes +FollowSymLinks
        AllowOverride None
        Require all granted
    </Directory>

    # Reverse proxy settings to forward requests to the Node.js application
    ProxyRequests Off
    ProxyPreserveHost On

    # Exclude the actual directory and file access from proxying so Apache serves them directly
    ProxyPass /index.js !
    ProxyPass /admin.js !
    ProxyPass /.env !
    ProxyPass /uploads !

    # Proxy the search and admin routes to the Node.js backend
    ProxyPass /search http://127.0.0.1:3000/search
    ProxyPassReverse /search http://127.0.0.1:3000/search

    ProxyPass /admin http://127.0.0.1:3000/admin
    ProxyPassReverse /admin http://127.0.0.1:3000/admin

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
